@* Modelo de donde vamos a coger los datos *@
@model IEnumerable<EFconASPyMVC.Models.Alumno>
@{
	ViewData["Title"] = "AlumnoPDF";
}
@{
	Layout = null;
}
<!DOCTYPE html>
<html>
<head>
	<meta name="viewport" content="width=device-width" />
	<title>AlumnoPDF</title>
	<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
	<link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
	<div class="row">
		<div class="col-sm-8 text-right">
			<h2>Lista de Alumnos</h2>
		</div>
	</div>
	<table class="table">
		<thead>
			<tr>
				@* Nombres de los campos de la tabla *@
				<th>
					@Html.DisplayNameFor(model => model.Id)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Nombre)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Edad)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.ClaseId)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Usuario)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.AlumnoTareas)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Clase)
				</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				@*Valores de los campos en cada registro de la tabla *@
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Id)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Nombre)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Edad)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.ClaseId)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Usuario)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.AlumnoTareas)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Clase)
					</td>
				</tr>
			}
		</tbody>
	</table>
</body>
</html>
